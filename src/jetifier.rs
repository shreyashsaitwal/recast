use std::path::Path;

/// Jetifies the AndroidRuntime.jar, i.e., migrates any references to the support library packages
/// to their AndroidX equivalent.
pub fn jetify(base_dir: &Path) {
    // todo
}
